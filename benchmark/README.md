# Benchmarks

Benchmarks will report complete workload latency, resident kernel latency,
memory use, and provenance. Results are meaningful only with the model,
quantization, device, compiler, and workload shape recorded beside them.
