# Kernels

`reference` contains correctness-oriented kernels and `specs` contains kernel
contracts. Generated implementations belong in `generated` only when their
generator, input, and provenance are recorded.
